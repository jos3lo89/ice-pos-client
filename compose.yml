services:
  ice-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ice-frontend
    ports:
      - "4000:80"
    environment:
      - VITE_API_BASE_URL=${VITE_API_BASE_URL}
      - VITE_API_PRINTER=${VITE_API_PRINTER}
    restart: unless-stopped